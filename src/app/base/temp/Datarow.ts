export default interface dataRow{ //will change in the future
  index: number;
  content: string;
  humanLabel: string;
  predictions: string;
  status: string;
  isSelected: string;
  isEnable: string;
}
